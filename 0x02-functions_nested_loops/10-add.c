#include "holberton.h"
#include <stdio.h>

/**
* add - function that adds two integers and returns the result
*
* Return: the sum
*/

int add(int a, int b)
{
	int result = a + b;

	return (result);
}
