#include "holberton.h"
#include <stdio.h>

/**
* add - function that adds two integers and returns the result
* @a: first number
* @b: second number
*
* Return: the sum
*/

int add(int a, int b)
{
	int result = a + b;

	return (result);
}
