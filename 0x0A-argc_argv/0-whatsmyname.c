#include "holberton.h"
#include <stdio.h>

/**
* main - prints program name + \n
* @argc: argument count
* @argv: argument values
*
* Return: always 0
*/

int main(int __attribute__((unused))argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
